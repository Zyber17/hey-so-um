{
  "name": "hey-so-um",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.11"
  },
  "scripts": {
    "start": "react-scripts start",
    "test": "echo 'no tests'",
    "eject": "react-scripts eject",

    "build": "react-scripts build",
    "deploy": "surge --project ./build --domain hey-so-um.zcorbett.com",

    "precommit": "npm test",
    "prepush": "npm test"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "surge": "^0.19.0"
  }
}
